# Outputs

Each production/shadow run will eventually write to a dated directory:

    outputs/YYYY-MM-DD/

Runs are immutable snapshots and should not overwrite earlier decisions.
Generated outputs remain local by default.